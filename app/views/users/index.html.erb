<h1>Users</h1>

<p class="strapline"> Below is a list of all users </p>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to(user.username, username) %>
    </li>
  <% end %>
</ul>

<%= link_to "New User", new_user_path %>